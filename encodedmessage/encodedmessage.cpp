#include <bits/stdc++.h>
using namespace std;

int main() {

    int n;
    while(n > 0) {
        string s;
        cin >> s;
        vector<vector<char>> vc;
        for (int i = 0; i < s.size() % 5; i++) {
            for (int j = 0; j < 5; j++) {

            }
            vc.emplace_back(Args &&args...)
        
        }
        n--;
    }

    return 0;
}
